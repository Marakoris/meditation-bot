# 🧘 Telegram Meditation Bot

Бот для отслеживания медитаций с поддержкой индивидуальных практик и марафонов. Интегрирован с ИИ для персональной обратной связи.

## ✨ Функции

### Для пользователей:
- **Индивидуальные медитации**: отслеживание времени, комментарии, оценки
- **Марафоны**: групповые практики с целями и статистикой
- **Персональная обратная связь** от ИИ после каждой медитации
- **История медитаций** и прогресс
- **Напоминания** для участников марафонов

### Для администраторов:
- Создание и управление марафонами
- Просмотр групповой статистики
- Автоматические отчеты по завершении марафонов

## 🚀 Быстрый старт

### 1. Создание бота

1. Откройте [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте `/newbot` и следуйте инструкциям
3. Сохраните полученный токен

### 2. Настройка окружения

```bash
# Клонируйте репозиторий
git clone <your-repo-url>
cd meditation-bot

# Создайте файл .env на основе примера
cp .env.example .env

# Отредактируйте .env и добавьте свои данные:
# - BOT_TOKEN (токен от BotFather)
# - DATABASE_URL (URL PostgreSQL)
# - AI_API_KEY (ключ Claude или OpenAI)
# - ADMIN_IDS (ваш Telegram ID)
```

### 3. Запуск с Docker

```bash
# Запустите контейнеры
docker-compose up -d

# Проверьте логи
docker-compose logs -f bot
```

### 4. Запуск без Docker

```bash
# Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# Установите зависимости
pip install -r requirements.txt

# Запустите бота
python bot.py
```

## 📁 Структура проекта

```
meditation-bot/
├── bot.py              # Основной файл бота
├── config.py           # Конфигурация
├── database.py         # Работа с БД
├── keyboards.py        # Клавиатуры
├── ai_service.py       # Интеграция с ИИ
├── utils.py            # Утилиты и менеджеры
├── requirements.txt    # Зависимости
├── Dockerfile          # Docker образ
├── docker-compose.yml  # Docker Compose конфигурация
└── .env.example        # Пример переменных окружения
```

## 🛠️ Конфигурация

### Обязательные переменные окружения:

- `BOT_TOKEN` - токен бота от BotFather
- `DATABASE_URL` - URL для подключения к PostgreSQL
- `AI_API_KEY` - ключ API для Claude или OpenAI

### Опциональные настройки:

- `AI_SERVICE` - выбор ИИ провайдера (claude/openai)
- `ADMIN_IDS` - список ID администраторов
- `TIMEZONE` - часовой пояс
- `MAX_SESSIONS_PER_DAY` - максимум медитаций в день

## 💾 База данных

Бот использует PostgreSQL со следующими таблицами:

- **users** - информация о пользователях
- **sessions** - сессии медитаций
- **marathons** - информация о марафонах
- **marathon_participants** - участники марафонов

Таблицы создаются автоматически при первом запуске.

## 🤖 Интеграция с ИИ

### Claude API
1. Получите ключ на [console.anthropic.com](https://console.anthropic.com/)
2. Установите `AI_SERVICE=claude` в .env
3. Добавьте ключ в `AI_API_KEY`

### OpenAI API
1. Получите ключ на [platform.openai.com](https://platform.openai.com/)
2. Установите `AI_SERVICE=openai` в .env
3. Добавьте ключ в `AI_API_KEY`

## 📱 Команды бота

### Для пользователей:
- `/start` - запуск бота
- **🧘 Начать медитацию** - начало новой сессии
- **⏹️ Завершить медитацию** - завершение текущей сессии
- **🏃 Присоединиться к марафону** - просмотр и присоединение к марафонам
- **📊 Мой прогресс** - статистика и прогресс
- **📖 История медитаций** - последние медитации

### Для администраторов:
- **👨‍💼 Создать марафон** - создание нового марафона

## 🚀 Деплой

### Railway
1. Создайте новый проект на [railway.app](https://railway.app)
2. Подключите GitHub репозиторий
3. Добавьте PostgreSQL из маркетплейса
4. Установите переменные окружения
5. Деплой произойдет автоматически

### Render
1. Создайте Web Service на [render.com](https://render.com)
2. Подключите репозиторий
3. Добавьте PostgreSQL базу данных
4. Настройте переменные окружения
5. Запустите деплой

### VPS/Dedicated Server
```bash
# Установите Docker и Docker Compose
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh

# Клонируйте репозиторий
git clone <your-repo-url>
cd meditation-bot

# Настройте .env
cp .env.example .env
nano .env

# Запустите
docker-compose up -d
```

## 🔧 Разработка

### Добавление новых функций

1. **Новые команды**: добавьте обработчик в `bot.py`
2. **Новые таблицы БД**: расширьте `create_tables()` в `database.py`
3. **Новые клавиатуры**: добавьте функции в `keyboards.py`
4. **Новые ИИ провайдеры**: создайте класс в `ai_service.py`

### Тестирование

```python
# Запустите бота в режиме отладки
import logging
logging.basicConfig(level=logging.DEBUG)
```

## 📝 Примеры использования

### Индивидуальная медитация
1. Нажмите "🧘 Начать медитацию"
2. Медитируйте
3. Нажмите "⏹️ Завершить медитацию"
4. Введите комментарий о практике
5. Выберите оценку от 1 до 10
6. Получите персональную обратную связь

### Участие в марафоне
1. Нажмите "🏃 Присоединиться к марафону"
2. Выберите активный марафон
3. Выполняйте ежедневную цель
4. Отслеживайте прогресс через "📊 Мой прогресс"
5. Получите итоговый отчет после завершения

## 🐛 Решение проблем

### Бот не отвечает
- Проверьте правильность `BOT_TOKEN`
- Убедитесь, что бот запущен: `docker-compose ps`
- Проверьте логи: `docker-compose logs bot`

### Ошибки базы данных
- Проверьте `DATABASE_URL`
- Убедитесь, что PostgreSQL запущен
- Проверьте права доступа к БД

### Нет обратной связи от ИИ
- Проверьте `AI_API_KEY`
- Проверьте баланс API аккаунта
- Посмотрите логи на наличие ошибок API

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для фичи (`git checkout -b feature/AmazingFeature`)
3. Закоммитьте изменения (`git commit -m 'Add AmazingFeature'`)
4. Запушьте ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

Распространяется под лицензией MIT. См. `LICENSE` для подробностей.

## 👥 Контакты

- Telegram: [@your_username](https://t.me/your_username)
- Email: your.email@example.com

---

Сделано с ❤️ для практикующих медитацию
